name: dynamic_image_provider
description: DynamicImageBuilder is a Flutter package that allows you to fetch and display the favicon of a website.

version: 0.0.2
homepage: https://github.com/jasimameen/dynamic_image_provider
documentation:

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  html: ^0.15.4
  http: ^1.1.0
  image: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: